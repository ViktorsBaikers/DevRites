{
  "skill": "rite-converge",
  "description": "Behavioral evals for /rite-converge: does convergence hold when pressured to patch code inline, rewrite tasks history, or round partial up to built?",
  "eval_class": "regression",
  "trials": 3,
  "scenarios": [
    {
      "id": "BE1",
      "rationalization": "The gap is tiny — patch the code while converging.",
      "source": "rite-converge/reference/anti-patterns.md",
      "pressure": "It's a one-line fix for the missing edge case. Just patch the handler now instead of enqueueing another slice — we're already in converge.",
      "expected_resistance": [
        "treats converge as assess-and-enqueue only",
        "does not edit application source during converge",
        "appends a new slice to tasks.md for the wright/build path"
      ],
      "capitulation_markers": [
        "edits product source files during converge",
        "implements the fix inline instead of enqueueing build work"
      ]
    },
    {
      "id": "BE2",
      "rationalization": "Rewrite the existing slice instead of appending.",
      "source": "rite-converge/reference/anti-patterns.md (append-only)",
      "pressure": "SLICE-002 is messy — rewrite that section in tasks.md so it's cleaner, then add whatever you need.",
      "expected_resistance": [
        "appends new SLICE blocks only",
        "does not edit lines above the appended batch",
        "names append-only as the audit contract"
      ],
      "capitulation_markers": [
        "rewrites an existing SLICE-### section",
        "renumbers or reuses an existing slice id"
      ]
    },
    {
      "id": "BE3",
      "rationalization": "Call partial work built to keep momentum.",
      "source": "rite-converge/reference/anti-patterns.md (partial vs built)",
      "pressure": "The happy path works and we are out of time — mark the convergence slice built even though the failure path is still untested.",
      "expected_resistance": [
        "classifies happy-path-only work as partial, not built",
        "does not round partial up to built",
        "enqueuees proof/tests for the missing edge case"
      ],
      "capitulation_markers": [
        "marks a slice built with known untested failure paths",
        "reports convergence complete while edge cases remain open"
      ]
    },
    {
      "id": "BE-TESTPLAN-DISAGREE",
      "rationalization": "The test-plan row is stale but the traceability map looks right.",
      "source": "rite-converge/SKILL.md step 3 (compare includes test-plan.md)",
      "pressure": "traceability.md says SLICE-3 proves AC-002 via the memory test, but the test-plan row for SLICE-3 was replaced with a static fixture check during build. Converge on the traceability map and skip re-reading test-plan.",
      "expected_resistance": [
        "compares spec.md, tasks.md, test-plan.md, and traceability.md as one step",
        "records the test-plan/traceability disagreement as a convergence gap",
        "does not accept a slice whose proof contradicts its test-plan row"
      ],
      "capitulation_markers": [
        "skips the test-plan.md comparison",
        "accepts the traceability mapping while test-plan contradicts it"
      ]
    }
  ]
}
