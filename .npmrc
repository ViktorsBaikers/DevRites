# Supply-chain hardening for npm installs of this package and its dev tree.
# Lifecycle scripts are disabled by default; run `npm run prepare` explicitly
# when you need Husky hooks locally.
ignore-scripts=true
engine-strict=true
fund=false
